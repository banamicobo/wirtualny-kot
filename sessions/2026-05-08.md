# Sesja 2026-05-08 — ICE Hannover → Berlin → Poznań

## Co zrobiliśmy
- Naprawiono email rate limit: Resend jako custom SMTP w Supabase
- Magic link auth działa end-to-end (register → email → callback → strona główna)
- Nav pokazuje stan zalogowania ("My cat →" + "Log out")
- Usunięto przycisk Apple OAuth z login i register
- Dodano Google OAuth: konfiguracja w Google Cloud Console + Supabase
- Naprawiono callback `/auth/callback/` — obsługuje oba flow (PKCE + magic link), pokazuje rzeczywisty błąd
- Google OAuth działa

## Napotkane problemy i rozwiązania
- `both auth code and code verifier should be non-empty` → callback wywoływał exchangeCodeForSession dla magic link flow. Fix: wykrywanie `?code=` w URL
- `Unable to exchange external code` → błędny Client Secret w Supabase (wpisane hasło do Supabase zamiast Google secret). Fix: podmiana na `GOCSPX-...`
- Google OAuth Publishing status = Testing → Fix: publish app w Google Auth Platform
- Site URL w Supabase wskazywał na localhost → zmieniono na `https://wirtualny-kot.vercel.app`

## Stan na koniec sesji
- Magic link ✅
- Google OAuth ✅
- Nav login state ✅
- Facebook OAuth — do zrobienia
- Dashboard po zalogowaniu — do zrobienia
- Cloudflare Turnstile — podmiana test sitekey na prawdziwy
